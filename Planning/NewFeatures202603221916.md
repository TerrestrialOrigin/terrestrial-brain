- [ ] Test the AI Output and polling to make sure AI can place a note of its creation into any folder
- [ ] Remove the AI notes folder setting from the plugin since the AI Output will provide a full path and should be able to place the note anywhere (via the Obsidian plugin pull)
- [ ] Change the Sync delay and AI notes poll interval to be in minutes, not milliseconds (just do the conversion to milliseconds as needed in the code)
- [ ] Make the pulling notes easier to access: keep the Ctrl + P options, but make the brain icon show a context menu with the "Sync note to Terrestiral Brain", and "Pull AI Output from Terrestrial Brain"
- [ ] Make the MCP function that allows AI to create output reject the output if the filepath is invalid, like if it has invalid characters. What's the most restrictive OS? Windows? I remember windows rejecting certain notes because they had funny characters in the title. Basically any invalid path, reject it with a descriptive error message so that AI can try again with a proper path this time.