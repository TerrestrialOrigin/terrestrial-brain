Please start a new openspec change that will include the enahancements listed below. Do all the openspec stuff yourself, after the code chagnes are done, write any new tests you need to write, make sure ALL existing tests are ran and pass without exception, the archive and sync openspec change, then commit everything and merge with dev.

- [ ] Change the "chars" display to actual storage size in KB, MB, GB, or whatever's appropriate: Currently when the dialog comes up that tells the users that there are pending AI outputs that can be pulled down (one with "accept all" and "reject all" buttons), it has "# chars" at the bottom of the row. This needs to change to say the actual size of the file in proper unit like bytes, KB, MB, etc (depending on the size, display the appropriate unit so that the user doesn't ahve to do the math themselves).
- [ ] Make sure that the new AI output doesn't fully get pulled down to the user's computer until the user accepts it. This means that the plugin should only get the full path of the file where the ouput would be placed + the size of the output but not the output itself until the user clicks "accept". This is because the purpose of this dialog is to prevent malicious output that's meant to clog up the heard drive or otherwise overload the user's computer. If the output gets sent to the users machine PRIOR to them accepting it, that kind of defeats the purpose (it would overrun the memory instead of the hard drive space, which is still bad). So have the MCP calculate the size of each output file and send that and the file path to the user, and then only when the user clicks "accept all" should the obsidian plugin actually pull the body of the note.
- [ ] When the user manually does the "pull ai output" function (but not when it's done automatically in the background), if there's nothing to pull down, there should be an informative dialog that tells user that, otherwise the user doesn't know if there was nohting to pull down or things are broken.