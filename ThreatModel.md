# Threat Model — Terrestrial Brain

Living document. Each OpenSpec change with security impact records its analysis here
(started by change `header-based-auth`, 2026-07-04).

## Trust model (current)

- **Security boundary:** a single shared secret, `MCP_ACCESS_KEY`, verified by the
  `terrestrial-brain-mcp` edge function on every request (MCP and direct HTTP routes alike).
- **Transport of the secret:** `x-tb-key` request header (primary). `?key=` query
  parameter (deprecated) is retained only for MCP clients that cannot set custom headers.
  The secret is deliberately carried in a dedicated custom header rather than
  `Authorization: Bearer` — this keeps the app-level shared secret separate from the
  Supabase/Kong gateway's own `Authorization`/JWT channel, avoiding a gateway-level
  collision where the platform would try to validate the key as a JWT (recorded by
  change `ob1-fragment-rewrite`).
- **Database access:** the edge function uses the Supabase service-role key exclusively.
  Row-Level Security policies exist to lock the public anon key out of ALL data and
  privileged functions (enforced by change `fix-db-security-policies`); RLS is NOT a
  per-user authorization layer.
- **Tenancy:** single-tenant, single shared key. There is no per-user identity, key
  rotation, or scoping. This is an accepted design constraint for a personal system.

## Threats and mitigations

| # | Threat | Status | Mitigation / Rationale |
|---|---|---|---|
| T1 | Timing attack recovers `MCP_ACCESS_KEY` through the key comparison | **Mitigated** (header-based-auth) | Comparison hashes both values to SHA-256 digests and compares with a branch-free XOR fold — no short-circuit, no length leak (`terrestrial-brain-mcp/index.ts`, `accessKeyMatches`) |
| T2 | Key disclosure via URL surfaces (proxy/CDN/edge logs, Referer headers, browser history, screenshots) | **Mitigated (default)** (edge-security-residual) | The `?key=` query-param fallback is now **rejected by default** — the `x-tb-key` header is the only accepted credential unless an operator explicitly sets `TB_ALLOW_KEY_IN_QUERY=1` for a client that cannot send headers. The plugin already sends the key only as a header and auto-migrates legacy `?key=` out of stored settings. With the flag off (the shipping default) no client can put the key in a URL; the residual applies only to installs that deliberately opt in. Guarded by `resolveProvidedKey` (`security-config.ts`) + auth deny tests (`tests/integration/auth.test.ts`) |
| T3 | Cleartext interception of notes + key over plain HTTP | **Surfaced** (header-based-auth) | Settings tab shows a persistent warning for `http://` endpoints on non-local hosts (`localhost`/`127.0.0.1` exempt). Not hard-blocked: LAN/self-hosted setups are legitimate |
| T4 | Key theft from Obsidian plugin data (`data.json` is unencrypted; a malicious plugin could read it) | **Accepted, documented** | Standard Obsidian practice; called out in README's warning block. Revisit if Obsidian adds a secrets API |
| T5 | Anon-key access to database tables / privileged functions | **Mitigated** (fix-db-security-policies) | RLS policies scoped `to service_role`; DML + EXECUTE revoked from `anon`/`authenticated`, with default privileges altered for future objects |
| T6 | Brute-forcing the access key | **Accepted (low risk)** | Key is operator-generated high entropy (README setup instructs a random string). No rate limiting at the edge — revisit if abuse is observed (function-call logs record caller IPs) |
| T7 | Cross-origin browser calls (wildcard CORS lets any page script the endpoint) | **Mitigated** (edge-security-residual) | CORS now **defaults to deny**: the app reflects `Access-Control-Allow-Origin` only for origins in the operator-configured `TB_ALLOWED_ORIGINS` allowlist (empty by default → no cross-origin), and never emits `*`. The Obsidian plugin and MCP clients are not browsers and need no CORS. Auth remains the authoritative gate regardless (a cross-origin page without the key still gets 401). Verified in-process against the real middleware (`tests/unit/cors-middleware.test.ts`). **Note:** the local Supabase dev gateway (Kong) injects permissive `*` on `/functions/v1/*`, masking the app on the local network path; hosted deployments are app-authoritative (functions own their CORS), which is why the assertion is made in-process rather than through the local stack |
| T8 | Prompt injection via ingested note content into the LLM extraction pipeline | **Partially addressed** | The Slack ingest surface was removed (`remove-slack-integration`); remaining ingest paths all require the access key. LLM-driven destructive writes are being removed separately (fix-plan Step 4: soft-archive instead of hard delete) |
| T9 | Missing required secret silently degrades the function (`Bearer undefined`, broken auth) | **Mitigated** (fail-fast-env-and-errors) | `requireEnv` throws at cold start naming the missing variable; the function refuses to boot rather than run with an undefined secret |
| T10 | Secret disclosure through surfaced error text | **Mitigated** (fail-fast-env-and-errors) | `requireEnv` throws with the variable *name* only, never its value; the `(section unavailable: <reason>)` marker echoes only the Supabase error message (schema/constraint text), no secret material |
| T11 | Surfaced DB error messages leak internal schema detail to a caller | **Accepted (low risk)** (fail-fast-env-and-errors) | Caller is already past the shared-secret gate; the marker exposes no more than the existing top-level `catch`/`console.error`. No new external surface |
| T12 | Destructive erasure abuse via `forget_note` / `/forget-note` (permanent hard-delete of a note's snapshot + thoughts) | **Mitigated** (gdpr-data-lifecycle) | Same `x-tb-key` gate as every other route (401 without it); scoped to a single `reference_id` — no bulk/wildcard delete; idempotent, so a replay does no extra damage; never reachable from an LLM path (contrast the LLM reconciliation path, which can only soft-archive per fix-plan Step 4) |
| T13 | Indefinite retention of personal note content + caller IPs in `function_call_logs` (GDPR data minimization) | **Mitigated** (gdpr-data-lifecycle) | Rows purged after a retention window via `purge_function_call_logs` (default 90 days, scheduled where `pg_cron` is available; EXECUTE service-role-only); serialized log input capped at 10,000 chars so a single row cannot accumulate unbounded content |
| T14 | New `function_call_logs.returned_ids` column accumulates linkable personal data / widens a data surface (GDPR data minimization) | **Mitigated** (records-returned-telemetry) | Column stores opaque entity UUIDs **only**, never thought/note content; written only by thought-retrieval reads, bounded by `MAX_QUERY_LIMIT = 100` ids per row; lives in the same service-role-only, RLS-protected table and is purged by the same 90-day `purge_function_call_logs` window as every other column. No new auth surface (same `x-tb-key` gate), no external consumer, and the value is stripped from the MCP client payload (never leaves the server) |
| T15 | Memory-mechanism audit against the live production DB corrupts data or leaks the Management API access token | **Mitigated, procedural** (memory-mechanism-audit) | The audit runbook (`docs/usefulness-audit-runbook.md`) is Ground-Rule-enforced READ-ONLY: every query is a `SELECT`, no `UPDATE`/`DELETE`/`INSERT` runs during an audit, and any needed fix is filed as a task rather than hot-fixed against prod. The access token is read from the system keyring into a shell variable and is never echoed, written to a file, committed, or pasted into the report. Enforcement is procedural (the token is the operator's own CLI token with full rights) — the control is the discipline codified in the runbook and this row, not a technical write-block |
| T16 | Mutation-ruleset bypass: a future memory console or PMS connector writes memory directly, skipping the one server-side update path, evading re-embed/re-hash and the dedup gate (stale search + duplicate leak — the disease the product cures) | **Mitigated** (memory-hygiene) | Step 7 implements INVARIANT 1 in the single server-side update path: every content edit re-embeds (thoughts) + re-hashes (`content_hash` on thoughts/projects/tasks/documents), and every mutation records `last_actor` (LLM \| user \| sync) through that one path. The write-time dedup gate runs on every create path. Test-guarded by the `lifecycle-rules-verification` deterministic tier (INVARIANT-1 hash-equals across all four entities, actor recorded per path, dedup blocks duplicates) |
| T17 | Poisoned extraction: a hallucinated or injected `type` (or other metadata) flows unvalidated into a stored mutation | **Mitigated** (memory-hygiene) | Step 7 parses `extractMetadata` output against the `THOUGHT_TYPES` allowlist (extended with `instruction`/`decision`) in `coerceThoughtType`, coercing an out-of-allowlist or missing value to the `observation` fallback and logging it — `parse, don't cast` at the seam. Test-guarded (out-of-allowlist → `observation`; allowlisted preserved) |
| T18 | Silent status divergence: a UI or sync shortcut closes a PMS-origin task locally only, so TB and the PMS disagree without a consented decision | **Specified** (integration-sync-rules) | The sync spec mandates consented close + stays-open-on-failure/decline and single-owner status precedence (PMS owns PMS-origin status); `test`-tagged. Implementation deferred to the v1.5+ connector; the rule is fixed now |
| T19 | Destructive supersession: contradiction handling that deletes/overwrites the older belief loses the audit trail and is unrecoverable | **Mitigated** (memory-hygiene) | Step 7 implements supersession as a retained `thoughts.superseded_by` edge: `search_thoughts_by_embedding` excludes superseded thoughts from default retrieval while they stay fetchable by id, and the `resolve_supersession` tool sets/clears the edge (fully reversible) — never delete/overwrite. Test-guarded on the effect (superseded thought leaves default search, row kept, edge reversible). Contradiction *detection* (which thought to supersede) is model judgment on the opt-in eval tier |
| T20 | Webhook replay/forgery: at-least-once delivery re-triggers extraction or a forged event mutates memory | **Specified** (integration-sync-rules) | The sync spec requires a cursor + content-hash idempotency gate (duplicate/trivial-edit events are no-ops) and connector secret validation; `test`-tagged on idempotent replay. Implemented with the first connector (v1.5+) |
| T21 | Vacuous-green verification harness: a lifecycle test that mocks the very behavior it claims to verify (e.g. faking the dedup decision) would pass without the feature existing, laundering unsafe behavior as safe | **Mitigated** (lifecycle-rules-test-harness) | The deterministic tier asserts on durable DB state (row counts, `metadata.type`, `usefulness_score`, column/tool existence) via the service role, never on prose; the only permitted mock is the external-LLM seam (`extractMetadata`), and only where the rule is *about* parsing LLM output. Shipped-behavior tests are mutation-checked (removing the implementation line reddens them). Enforced by the mock-boundary rule in review |
| T22 | Dishonest red: a red-by-design failure masking a genuine harness bug (a crash, wrong route, or typo) so a real gap hides behind the intended red | **Mitigated** (lifecycle-rules-test-harness) | Every pending test fails for exactly one documented reason via a `PENDING(<milestone>:<slug>)` marker in its name and assertion message; the RED reason is verified at apply-time to match the marker (not an unrelated error). Capability-absent tests anchor on explicit `hasTool` / `columnExists` probes rather than ambiguous unknown-tool errors |
| T23 | Silent coverage gap: a Step 5 scenario with no executable check gives false confidence that a rule is verified | **Mitigated** (lifecycle-rules-test-harness) | `tests/unit/lifecycle-coverage.test.ts` parses both delta specs and asserts a bijection with `tests/lifecycle-coverage.manifest.ts`; any uncovered, renamed, or removed scenario fails the build. The manifest also logs the red→green burn-down (pass-now / pending-step7 / pending-v1.5) |
| T24 | Unbounded aggregate read: `list_open_tasks_by_project` scans incomplete tasks across ALL projects, so a naive fetch-all could scan/serialize the whole `tasks` table (resource-exhaustion / slow-query surface), and grouping could leak content into logs | **Mitigated** (list-open-tasks-by-project) | Read-only `SELECT`, same `x-tb-key` gate as every tool (no new auth surface). Bounded: `limit` is Zod-validated (default 500, **max 1000**) and the query fetches only `limit + 1` rows; past the cap the response reports truncation and it is logged (never a silent fetch-all). Telemetry records ids + counts only, never task content. Filters are bound through the query builder, not string-concatenated. Deny-path shares `tests/integration/auth.test.ts`; bounds/exclusions covered by `tests/integration/open_tasks_by_project.test.ts` |

| T25 | Provisioning surface (Step 10, `provisioning-automation`, hosted-side): a leaked privileged Management API token grants full control of every customer project; an unvalidated region / customer id could be injected into Management API calls (SSRF/misplacement); a weak or reused per-customer key; secrets leaking into logs; crashes leaving orphaned projects; the interim job store holding minted keys in plaintext | **Mitigated (by design; implemented in the private `terrestrial-brain-hosting` repo)** | The provisioning pipeline lives in a separate PRIVATE repo (never in this FSL public tree). Controls: Management token is env-only, never logged, sent as `Authorization: Bearer`, never in a URL; region is a parse-don't-cast allowlist and customer ids are format-validated before any API call (anti-injection); per-customer MCP keys are CSPRNG-minted (256-bit, base64url), unique, and minted exactly once; a redacting logger + a no-secret-in-logs test keep secret values out of log output; the external project ref + db password are persisted BEFORE creation is confirmed and an explicit `rollback` op (opt-in auto-rollback) tears down partial provisions, so crashes leave a recoverable `failed` job not a forgotten orphan; the interim file job store is 0600 and outside any repo tree, documented as replaced by Step 13's control-plane secret storage. Verified by the hosting repo's deterministic suite (idempotency, crash-resume, interleave, rollback, no-secret-in-logs) with GATE-2b mutation checks; real-system confidence via an opt-in, fail-loud live smoke. No change to this repo's runtime, auth, or input paths |
| T26 | Fleet-operations surface (Step 11, `fleet-operations`, hosted-side): fleet-wide operations fan out with the privileged Management token, so its blast radius is now the whole fleet; the per-project read-only cross-project queries could be an injection or customer-content-leak surface; a dishonest monitor could swallow a failed probe into "healthy"; a dishonest sweep could hide a failed project behind a green run; a stale/wrong migration set could be applied fleet-wide; concurrent sweeps could race on one project | **Mitigated (by design; implemented in the private `terrestrial-brain-hosting` repo)** | Like Step 10, fleet ops live in the separate PRIVATE repo (never in this FSL public tree) and reuse Step 10's secret handling: the Management token is env-only, never logged, `Authorization: Bearer`, never in a URL. Controls: fleet enumeration is the job store's own `done` jobs (not "every org project"), so migrations are only ever applied to projects TB provisioned; the per-project inspector exposes NO generic SQL — only two purpose-specific, fixed, hosting-authored, bounded reads (applied-migration versions; an aggregate error count over a time window), whose only inputs are a path-encoded project ref and a numeric timestamp, and which return ids/counts only, never note/thought content; the monitor has an explicit `unreachable` bucket so a failed probe/query is preserved with its error, never coerced to healthy-0; the migrate sweep returns a per-project counted outcome (`in_sync`/`migrated`/`failed`) and reports overall failure (non-zero exit) if any project failed, counting success only from projects that actually ended in sync; the only mutation is additive migrations (`db push`, idempotent, never delete-then-write) computed against the pinned `TB_MIGRATIONS_SOURCE`, with `--dry-run` for pre-review, so concurrent sweeps converge without a per-project lease (accepted, documented trade-off); the interim 0600 job store and its plaintext db passwords are unchanged from T25 (read only to apply migrations, never logged). Verified by the hosting repo's deterministic suite (drift/idempotency, dry-run, partial-failure counted outcome, empty-fleet, unreachable≠healthy, no-secret-in-logs) with GATE-2b mutation checks; real-system confidence via an opt-in, fail-loud live smoke. No change to this repo's runtime, auth, or input paths |
| T27 | Deprovision/export surface (Step 12, `deprovision-and-export`, hosted-side): ending a tenancy deletes a customer project with the privileged Management token (the one irreversible operation) and, unlike fleet ops' content-free reads, pulls the customer's FULL content into an export — so the failure modes are deleting before the data is safely out, deleting the wrong project, leaking exported content or the db password, and a dishonest run reporting a half-done deprovision as complete | **Mitigated (by design; implemented in the private `terrestrial-brain-hosting` repo)** | Like Steps 10/11, the code lives in the separate PRIVATE repo (never in this FSL public tree) and reuses their secret handling: the Management token is env-only, never logged, `Authorization: Bearer`, never in a URL. Controls: the destructive delete is **structurally gated on a verified export** — `DELETE_PROJECT` is unreachable until `VERIFY_EXPORT` re-reads the delivered artifact and confirms its size + checksum, so a truncated/failed delivery can never unlock the delete, and the export manifest is persisted BEFORE the delete so a crash resumes at the delete rather than losing the export (never delete-then-write); the delete is scoped to the single `projectRef` read from the customer's OWN provisioning record (no bulk/wildcard, never operator free-text) and is idempotent (a project already gone is a successful erasure); the per-customer db password used to run the dump is passed to the subprocess via the ENVIRONMENT (`PGPASSWORD`), never as a command-line argument (argv is world-readable via `ps`) and never logged; the export manifest is content-free (location + checksum + byte/row counts only — never note/thought content) and the artifact is delivered `0600`; a redacting logger + a no-secret/no-content-in-logs test keep both the db password and dumped content out of logs; the run reports `deprovisioned` ONLY when the export verified AND the delete succeeded, else a recoverable `failed` job with its error + non-zero exit (never a loop-end "done"); on success the provisioning job flips to `deprovisioned` so the fleet (Step 11) stops operating on the deleted project; a not-found or non-`done` customer is refused, never guessed. GDPR: the project deletion IS Art. 17 erasure and the delivered dump IS Art. 20 portability, complementing the per-note `forget_note` (T12). **Open item:** the delivered export is itself retained personal data — its retention/rotation policy is deferred to Step 13's control plane. Verified by the hosting repo's deterministic suite (verify-before-delete gate, crash-resume, idempotency/runs-twice, guards, export-only non-destructive, no-secret/no-content-in-logs) with GATE-2b mutation checks; real-system confidence via an opt-in, fail-loud, NON-destructive `export:live` smoke (the destructive path is manual, throwaway-only). No change to this repo's runtime, auth, or input paths |
| T28 | Control-plane surface (Step 13, `control-plane`, hosted-side): the durable control plane is ONE Supabase project that concentrates EVERY customer's secrets (minted MCP access keys, project db passwords) and the full customer→project→subscription map — the single highest-value target in the hosted system. Failure modes: a leaked control-plane service-role key granting total access; secrets exposed at rest or leaked through a customer-facing read/log; an injection path via a generic query surface; a race double-provisioning a customer (two projects / two charges); stale personal data lingering in delivered exports; malformed control-plane rows crashing far from cause | **Mitigated (by design; implemented in the private `terrestrial-brain-hosting` repo)** | Like Steps 10–12 the code lives in the separate PRIVATE repo (never in this FSL public tree). Controls: the control-plane service-role key is the crown-jewel credential — env-only, added to the redacting logger's secret set (never logged), sent in headers as `apikey`/`Authorization: Bearer`, never in a URL; every control-plane table has RLS enabled with NO policies and all grants revoked from `anon`/`authenticated`, so the tables are reachable only with the service role; the minted access key + db password live in a dedicated service-role-only `project_secrets` table, split off the provisioning row on save and rejoined only on load, and are NEVER returned by any customer-facing service read (customer list / connection lookup) nor logged; the `ControlPlaneClient` seam exposes ONLY intent-named, parameterised methods (no generic `query(sql)`), so there is no injection surface, and the atomic claim is a single Postgres conditional write (`claim_*_job`) so two concurrent claims for one customer are resolved by the database — exactly one wins (no double-provision); delivered exports are recorded content-free (location + checksum + byte/row counts + retention deadline) and `purge-exports` deletes both file and record for artifacts past `TB_EXPORT_RETENTION_DAYS` with a counted, honest outcome (this closes T27's deferred retention item); every control-plane row is parsed at the boundary (Zod) into a known-good type, a malformed/legacy row failing loudly rather than casting into a downstream crash. **Accepted, documented trade-off:** at-rest secret protection is Supabase platform-level (disk encryption) + strict access control, NOT app-level envelope encryption — the `SecretStore` seam makes a Vault/KMS backend a later swap with no caller change. This resolves T25's "interim plaintext store" open item (the file store is now a documented standalone-dev fallback; the hosted path is control-plane-backed). Verified by the hosting repo's deterministic suite (claim CAS incl. interleave one-winner + lease-reclaim, save/load with secrets rejoined, crash-resume, secret omission from reads, idempotent subscription upsert + refuse-unknown, export-retention only-due/counted/idempotent, parse-at-boundary rejection, no-secret-in-logs) with GATE-2b mutation checks; real-system confidence via an opt-in, fail-loud `control-plane:live` smoke (throwaway control-plane project). No change to this repo's runtime, auth, or input paths |
| T29 | Billing-webhook surface (Step 14, `paddle-billing`, hosted-side): an internet-facing, unauthenticated-by-default HTTP endpoint (`billing serve`) whose events trigger money-relevant and DESTRUCTIVE actions — a checkout provisions a project (spend), a cancellation deprovisions it (customer-data deletion), a payment failure pauses it. Failure modes: a forged webhook driving an unauthorised provision/deprovision; replay of a captured valid webhook; event/customer spoofing to deprovision a victim; the webhook signing secret leaking; a malformed payload crashing far from cause; a redelivery storm fanning out into duplicate provisions | **Mitigated (by design; implemented in the private `terrestrial-brain-hosting` repo)** | Like Steps 10–13 the code lives in the separate PRIVATE repo (never in this FSL public tree) and reuses their secret handling + composition root. Controls: **signature verification gates everything** — before any parse or side effect the receiver verifies the `Paddle-Signature` (`ts=…;h1=…`) as an HMAC-SHA256 over the timestamped **raw** body against `TB_PADDLE_WEBHOOK_SECRET`, compared in **constant time** (full-length, no early return); no valid signature ⇒ `401`, zero side effect. **Replay** is bounded by a signed-timestamp freshness window AND the at-least-once idempotency ledger: each Paddle `event_id` is claimed via a single atomic insert-if-absent into a service-role-only `webhook_events` table, so a redelivery (or replay) is a `200` no-op and each side effect runs exactly once (a retry storm cannot fan out into duplicate provisions). **Spoofing to deprovision a victim** is blocked because the customer id comes from the signed `custom_data` (a forged body fails the HMAC), the deprovision is scoped to the customer's OWN `projectRef`, and the Step-12 pathway **exports-and-verifies before it deletes** (no unverified destruction). The **webhook secret** is env-only, added to `secretValues()`/the redacting logger (never logged), never in a URL, used only inside the verifier. **Malformed payloads** are rejected at the boundary (Zod parse-at-boundary → `400`) and unrecognised event types are acknowledged-and-ignored (`200`, no action), so a bad body never flows an `as`-cast value into a mutation. An event for an unknown customer is **refused, never silently created**. All webhook logic is the transport-neutral `BillingService` (the `Deno.serve` receiver only maps the outcome to a status code), so it is fully testable with fakes. Verified by the hosting repo's deterministic suite (real HMAC against a known vector + tamper/wrong-secret/stale-ts rejection, constant-time compare, event-parser type mapping/no-customer refusal/unknown-type ignore, first-vs-redelivery claim, and the full checkout→provision / payment-failure→pause / cancellation→deprovision / resume→restore / idempotent-redelivery / signature-reject / bad-request / no-secret-in-logs integration paths) with GATE-2b mutation checks (verifier bypass, always-claim, dropped pause, dropped no-customer refusal all confirmed to redden their tests); real-system confidence via an opt-in, fail-loud, NON-destructive `billing:live` smoke (real signature + real `webhook_events` idempotency against a throwaway control-plane project). No change to this repo's runtime, auth, or input paths |
| T30 | Managed-AI metering surface (Step 15, `managed-ai-metering`, PUBLIC edge function): the hosted product injects a SHARED OpenRouter key as each project's per-project secret, so every AI call is Terrestrial's cost; without a cap one tenant can run up unbounded spend on the shared key. Secondary failure modes of the cap itself: a mis-set quota bricking a customer's AI (a `0` blocking everything); a metering outage silently disabling the cap; an over-quota search reading as an empty "brain is empty"; metering leaking personal content | **Mitigated (by design)** | This is a COST control, not an authorization control (auth stays the `x-tb-key` check, T2/T7 unchanged). Controls: a per-deployment monthly quota (`TB_AI_MONTHLY_LIMIT`) is enforced in the edge function on the dominant AI-consuming operations (`search_thoughts`, `capture_thought`, `ingest-note`), counted from the EXISTING `function_call_logs` telemetry with a single bounded, index-backed head-count over the current UTC month (no rows loaded, no content read — only `function_name` + `called_at` counts). An over-quota operation is refused **before any AI call** with a distinct, user-visible state — a `errorResult("AI quota exceeded: …")` (`isError:true`, logged `records_returned=0`) for MCP tools and a `429` for the `ingest-note` route — **never** a silent empty read (an over-quota search returns a quota error, not "no thoughts found"). Fail-safe defaults: the limit is parsed at the boundary and **unset / empty / non-numeric / ≤ 0 ⇒ unlimited**, so a self-hoster (own key) and a typo both default to unlimited rather than blocking all AI, and the unlimited path adds **no** usage query (zero behavior change vs. pre-Step-15). **Accepted, documented trade-offs:** (a) a transient metering-query failure **fails OPEN** (allows + logs) — the quota is best-effort cost control, not a security boundary, so an infra hiccup must not block a paying customer; it degrades to the pre-Step-15 uncapped behavior, never to a wrong block or empty; (b) metering is call-count granularity (not token-accurate); (c) the AI-consuming `write_document`/`update_document`/`update_thought` content path are a documented, unmetered v1 boundary (follow-up), not a silent gap. The quota-exceeded message carries only aggregate used/limit/reset — no note or thought content. Verified by unit tests (config parse incl. `0`/negative/non-numeric ⇒ unlimited, UTC-month window math, gate boundary `used <= limit`, null-limit short-circuit, fail-open, decorator skips the handler when over quota) and an integration test running the REAL meter/gate/decorator against REAL seeded `function_call_logs` (only in-window metered rows counted; out-of-window and non-metered excluded; boundary enforced; handler skipped over quota), with GATE-2b mutation checks (ignore-limit, dropped window filter, dropped function-name filter, no-skip decorator all confirmed to redden their tests). Default suite runs with the limit unset, so existing behavior is unchanged (backend 686 passed). |
| T31 | Onboarding surface (Step 16, `onboarding-flow`, hosted-side): an internet-facing signup + a session-gated read that reveals a **crown-jewel per-customer MCP access key** (the token that grants full access to a customer's brain). Failure modes: a stolen/guessed session token reading a victim's connection details; customer enumeration; the access key leaking (into logs, or to an unauthenticated caller); CSRF forcing a secret-returning GET; signup spam creating cost; a stale/expired session still resolving; a "ready" state revealing a blank/placeholder token | **Mitigated (by design; implemented in the private `terrestrial-brain-hosting` repo)** | Like Steps 10–14 the code lives in the separate PRIVATE repo (never in this FSL public tree) and reuses their control plane, secret store, redacting logger, and composition root. Controls: the **session token** is 256-bit CSPRNG (unguessable), stored ONLY as its SHA-256 **hash** (a control-plane DB read or leaked backup yields no live tokens), bound to ONE customer, with a bounded TTL (`TB_ONBOARDING_SESSION_TTL_DAYS`); resolution is a single bounded lookup by hash filtered on `expires_at_ms > now`, so an expired session resolves to nobody and the plaintext token never sits in a row to be compared byte-by-byte (no timing side channel). The token is carried in the **`x-tb-onboarding-token` header** — never a cookie (so a cross-site page cannot attach it → no CSRF on the secret-returning GET) and never a URL/query (no key-in-URL leak). **Access-key exposure** is confined to the transport-neutral `OnboardingService`: the key is read from the service-role-only secret store, revealed ONLY over a resolved session AND ONLY when the provisioning row is `done`, is the ONLY secret read (the db password is never read on this path), and is never logged (verified by a no-token/no-key-in-logs test). A ready-but-missing key is an explicit `error` (a broken state surfaced, `500`), **never** a package with a blank token. **Enumeration** is blocked because status/connection are keyed by the opaque token, never a customer id in the URL/body; signup returns the customer id only to the signer. **Signup abuse** creates a customer row + hashed session but **triggers no provisioning** (the Step-14 webhook is the sole provisioning trigger), so spam costs a row, not a project; rate-limiting/CAPTCHA is a documented follow-up. Parse-at-boundary (Zod) refuses a malformed email/region (`400`, no customer). The `Deno.serve` receiver holds no logic (maps outcomes to `201/200/409/401/400/500`). Verified by the hosting repo's deterministic suite (session-token hash known-vector + hashed-at-rest, connection-package shape with `x-tb-key`/no-dbPassword, state mapping across every provisioning×subscription case, handler routing/status codes, and the full signup→awaiting→real-provision→ready→connection integration incl. unauthorized/expired/forged token, not-ready-before-done incl. a running-with-key row, ready-but-no-secret error, no-secret-in-logs, and a compose-with-Step-14 webhook path) with GATE-2b mutation checks (drop the done-gate, accept-any-token, leak dbPassword-as-accessKey, ignore session expiry — all confirmed to redden their tests); real-system confidence via an opt-in, fail-loud, NON-destructive `onboarding:live` smoke (real CSPRNG token + real hashed-session round-trip against a throwaway control-plane project). No change to this repo's runtime, auth, or input paths. |

## Compliance notes (non-STRIDE)

- **Licensing & third-party attribution.** The public repository ships an
  explicit license (`LICENSE.md`, FSL-1.1-MIT) and a third-party attribution
  notice (`NOTICE.md`) for MIT-era Open Brain material. This closes a
  compliance gap (a public repo carrying third-party MIT material with no
  license file and no attribution) rather than a runtime attack surface — no
  code, input, or auth path is affected.
- **Marketing branding separated from provenance.** Public marketing surfaces
  (README headline, GitHub repository description) describe the product on its
  own terms with no Open Brain / OB1 / Nate reference; the required MIT
  attribution is retained in `NOTICE.md` and the README License section
  (change `branding-separation`). A deterministic docs-consistency test
  (`tests/unit/branding-separation.test.ts`) guards against reintroduction. No
  code, input, or auth path is affected.

## Out of scope (accepted for a single-tenant personal system)

- Multi-user authentication / authorization, per-client keys, key rotation schedules.
- Encryption at rest beyond what Supabase provides.
- Network-level controls (IP allowlists, mTLS).
